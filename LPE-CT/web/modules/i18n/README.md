This module owns locale configuration for the LPE-CT management console.

Responsibilities:
- define supported locales and labels
- define base and localized message catalogs
- create the shared i18n instance used by the UI

It does not own DOM rendering or application state.
