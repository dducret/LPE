# LPE 0.1.3

`0.1.3` is a schema reset release.

It removes the historical PostgreSQL migration chain and replaces it with a single canonical initialization script for fresh installs.

Breaking change:

- legacy updates are not supported
- a fresh install and database recreation are required
- startup now fails unless the database reports schema version `0.1.3`
