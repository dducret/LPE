# EWS Audit - 2026-06-02

## Scope

This audit compares the current `LPE` working tree against Microsoft's EWS operation catalog:

- <https://learn.microsoft.com/en-us/exchange/client-developer/web-service-reference/ews-operations-in-exchange>
- <https://learn.microsoft.com/en-us/exchange/client-developer/web-service-reference/ews-xml-elements-in-exchange>

It is a static repository audit plus local test verification. It is not a live Outlook, Exchange, Microsoft Remote Connectivity Analyzer, or Exchange Online compatibility run.

## Assumptions and Success Criteria

Assumptions:

- "EWS compatible" means compatibility with Microsoft's documented SOAP EWS operation surface, not only the subset needed by current Outlook profile creation.
- `LPE` should keep EWS bounded to canonical mailbox, contacts, calendar, tasks, submission, public-folder, rights, and synchronization state. EWS must not introduce parallel Outbox, Sent, mailbox, reminder, delegate, rule, or user-configuration state.
- SQL sufficiency means PostgreSQL has canonical durable state for the behavior, not that every Microsoft operation is already exposed through EWS.

Success criteria for this audit:

1. Recheck every EWS dispatcher operation currently visible in `crates/lpe-exchange/src/service.rs`.
2. Compare the visible operation surface with Microsoft's operation catalog.
3. Identify visible differences and implemented workarounds.
4. Verify whether implemented EWS operations route to canonical APIs/storage or stateless compatibility responses.
5. Verify whether SQL contains the data needed for the implemented bounded EWS surface and for obvious remaining high-value parity gaps.

## Summary

`LPE` still implements bounded EWS compatibility rather than full Microsoft Exchange Web Services compatibility, but the implemented surface has grown substantially since the previous audit snapshot.

Newly implemented dispatcher paths since the last audit include:

- Folder operations: `CreateFolderPath`, `CopyFolder`, `EmptyFolder`, `MoveFolder`, `UpdateFolder`.
- Mailbox item operation: `MarkAllItemsAsRead`.
- Conversation operations: `FindConversation`, `GetConversationItems`, `ApplyConversationAction`.
- Utility operations: `ConvertId`, `ExpandDL`, `GetMailTips`, `GetServiceConfiguration`, `GetUserPhoto`, `GetPasswordExpirationDate`, `MarkAsJunk`.
- Retention operation: `GetUserRetentionPolicyTags`.
- Sharing operations: `GetSharingMetadata`, `GetSharingFolder`, `RefreshSharingFolder`, and `CreateItem` with `AcceptSharingInvitation`.
- eDiscovery and transfer operations: `GetDiscoverySearchConfiguration`, `GetSearchableMailboxes`, `SearchMailboxes`, `GetHoldOnMailboxes`, `SetHoldOnMailboxes`, `GetNonIndexableItemDetails`, `GetNonIndexableItemStatistics`, `UploadItems`, and `ExportItems`.
- Delegate operations: `AddDelegate`, `GetDelegate`, `UpdateDelegate`, `RemoveDelegate`.
- User-configuration operations: `CreateUserConfiguration`, `GetUserConfiguration`, `UpdateUserConfiguration`, `DeleteUserConfiguration`.
- Mail app operations: `GetAppManifests`, `GetAppMarketplaceUrl`, `InstallApp`, `DisableApp`, `UninstallApp`, and `GetClientAccessToken`.
- Unified Messaging operations: `PlayOnPhone`, `GetPhoneCallInformation`, and `DisconnectPhoneCall`.

The automated EWS parity gate now accounts for every operation name in the Microsoft catalog snapshot. Every entry must name an existing SOAP test. Canonical implementations count toward behavioral coverage; unsupported entries must also pass an unsupported SOAP assertion and carry a tracked reason until canonical semantics exist.

Estimated current coverage:

- SOAP-tested Microsoft EWS operation-name coverage: 96/96 documented operation names, 100.0%.
- Canonical behavioral EWS coverage: 96/96 documented operation names, 100.0%.
- Explicit unsupported EWS SOAP coverage with tracked reasons: 0/96 documented operation names, 0.0%.
- Full behavioral/schema compatibility remains below operation-name coverage. The implemented operations are canonical and increasingly broad, but many remain deliberately partial compared with Exchange semantics.
- LPE's documented bounded EWS surface: implemented for the audited dispatcher paths.

## Current EWS Dispatcher Surface

The EWS dispatcher in `crates/lpe-exchange/src/service.rs` recognizes and dispatches these implemented operation names:

| Operation | Current LPE status | Canonical data/API path | Visible differences |
| --- | --- | --- | --- |
| `SyncFolderHierarchy` | Implemented, partial Exchange parity | Canonical mailboxes, contact/calendar/task collections, public-folder trees/folders | Emits current projected creates with synthetic sync state; not a full Exchange hierarchy sync engine. |
| `FindFolder` | Implemented, partial Exchange parity | Canonical mailboxes, collaboration collections, public-folder roots | Projects LPE folders and public-folder roots; not every Exchange distinguished/archive/search/public-folder shape. |
| `GetFolder` | Implemented, partial Exchange parity | Canonical mailbox and public-folder lookups | Supports selected distinguished folder ids, mailbox ids, and public-folder ids. |
| `FindItem` | Implemented, partial Exchange parity | JMAP mail, contacts, events, tasks, public-folder items | Bounded to canonical message/contact/event/task/public-folder post-like items. |
| `GetItem` | Implemented, partial Exchange parity | Same as `FindItem`; attachment fetches for MIME content | Supports LPE-prefixed ids only: `message:`, `contact:`, `event:`, `task:`, `public-folder-item:`. |
| `SyncFolderItems` | Implemented, partial Exchange parity | Current canonical item queries | Uses synthetic current-state sync tokens, not durable Exchange-equivalent EWS sync cursors. |
| `FindConversation` | Implemented, partial Exchange parity | Current canonical messages grouped by `thread_id` | Uses lightweight canonical message thread ids; no first-class Exchange conversation identity, lifecycle, or conversation index. |
| `GetConversationItems` | Implemented, partial Exchange parity | Current canonical messages grouped by `thread_id` | Returns current message nodes for requested conversations; folder ignore is bounded to current canonical folder membership. |
| `ApplyConversationAction` | Implemented, partial Exchange parity | Current canonical message move/delete/read-state APIs | Supports one-shot `Move`, `Delete`, and `SetReadState` over current messages in the thread; persistent future-message `Always*` actions return parseable gaps. |
| `GetServerTimeZones` | Implemented, partial Exchange parity | Static compatibility response | Minimal static time-zone set. |
| `ResolveNames` | Implemented, partial Exchange parity | Authenticated account, tenant directory, accessible contacts | No full Exchange GAL/template/persona behavior. |
| `FindPeople` | Implemented, partial Exchange parity | Authenticated account, tenant directory, accessible contacts | Stateless bounded persona projection only; no linked-person aggregation, merge state, profile photo, relevance graph, or Exchange persona store. |
| `GetPersona` | Implemented, partial Exchange parity | Same as `FindPeople` | Resolves LPE stateless `persona:account:{uuid}` and `persona:contact:{uuid}` ids only when the underlying account/contact remains visible. |
| `GetUserAvailability` | Implemented, partial Exchange parity | Canonical calendar events | Scoped to the authenticated mailbox's accessible calendar data. |
| `CreateItem` | Implemented, partial Exchange parity | Contact/event/task upserts; draft/import/submission; public-folder post create; canonical sharing grant acceptance | Does not implement every item class; `AcceptSharingInvitation` is bounded to same-tenant calendar/contact grants without Exchange sharing tokens; send dispositions use canonical submission. |
| `SendItem` | Implemented, partial Exchange parity | Existing canonical draft plus LPE submission/Sent path | Submits supported canonical draft ids only; no EWS-local Outbox or Sent copy. |
| `UpdateItem` | Implemented, partial Exchange parity | Contact/event/task updates, message read/flag updates, public-folder post update | Message updates remain limited to supported read, flag, and reminder state. |
| `DeleteItem` | Implemented, partial Exchange parity | Canonical contact/event/task/mail delete or Trash move; public-folder post delete | Delete types are mapped to LPE canonical delete/Trash behavior. |
| `ArchiveItem` | Implemented, partial Exchange parity | Canonical message move to the `archive` mailbox role | Moves canonical message ids into LPE's canonical Archive mailbox. This is not Exchange archive-mailbox or retention archive semantics. |
| `MoveItem` | Implemented, partial Exchange parity | Canonical message move; public-folder item clone+delete | Supports canonical message ids or public-folder item ids only. |
| `CopyItem` | Implemented, partial Exchange parity | Canonical message copy; public-folder item clone | Supports canonical message ids or public-folder item ids only. |
| `MarkAllItemsAsRead` | Implemented, partial Exchange parity | Canonical mailbox message read-state update | Bounded to supported mailbox folders; not a full Exchange public-folder/read receipt model. |
| `CreateFolder` | Implemented, partial Exchange parity | Canonical mailbox create; public-folder child create | Mailbox folder creation is custom-root oriented; public folder creation uses canonical public-folder storage. |
| `CreateFolderPath` | Implemented, partial Exchange parity | Canonical mailbox/public-folder path creation | Creates or reuses supported path segments; full Exchange atomic rollback semantics remain bounded. |
| `CreateManagedFolder` | Implemented, partial Exchange parity | Canonical managed retention folder API over `mailboxes` and `retention_policy_tags` | Creates or reuses root custom mailbox folders for active same-tenant visible or assigned custom/personal retention tags; no Exchange managed-folder policy blob or EWS-local folder state is created. |
| `CopyFolder` | Implemented, partial Exchange parity | Canonical mailbox and public-folder copy paths | Bounded recursive copy for supported custom mailbox/public-folder trees. |
| `EmptyFolder` | Implemented, partial Exchange parity | Canonical message delete/purge and public-folder item delete APIs | Supports bounded folder emptying and optional supported subfolder deletion. |
| `MoveFolder` | Implemented, partial Exchange parity | Canonical mailbox reparenting | Supports custom mailbox folders; public-folder move is rejected until canonical public-folder reparenting exists. |
| `UpdateFolder` | Implemented, partial Exchange parity | Canonical mailbox/public-folder update APIs | Updates supported display-name fields only. |
| `DeleteFolder` | Implemented, partial Exchange parity | Canonical mailbox destroy; public-folder delete | Protected/system semantics are delegated to storage. |
| `GetAttachment` | Implemented, partial Exchange parity | Canonical attachment content fetch | Supports LPE attachment references. |
| `CreateAttachment` | Implemented, partial Exchange parity | Magika validation plus canonical message attachment create | Supports only `FileAttachment` on one canonical message parent; no `ItemAttachment`. |
| `DeleteAttachment` | Implemented, partial Exchange parity | Canonical message attachment delete | Supports LPE attachment references. |
| `GetUserOofSettings` | Implemented, partial Exchange parity | Active canonical Sieve script | Projects vacation Sieve, not Exchange OOF storage. |
| `SetUserOofSettings` | Implemented, partial Exchange parity | Canonical Sieve script create/activate/deactivate | `Enabled` and `Scheduled` are represented as vacation Sieve metadata. |
| `GetInboxRules` | Implemented, partial Exchange parity | Canonical Sieve-backed mailbox rules | Projects bounded server-side rules only. |
| `UpdateInboxRules` | Implemented, partial Exchange parity | Canonical Sieve-backed rule mutation | Supports only rule shapes that map safely to canonical Sieve behavior. |
| `GetReminders` | Implemented, partial Exchange parity | Canonical calendar/task/message reminder projection | Computed reminder view; no Exchange reminder table. |
| `PerformReminderAction` | Implemented, partial Exchange parity | `reminder_occurrence_dismissals` and canonical reminder updates | Supports bounded dismiss/snooze behavior; unsupported actions return EWS errors. |
| `ConvertId` | Implemented, partial Exchange parity | Stateless canonical EWS id codec | Converts supported LPE EWS ids to/from deterministic opaque alternate ids; no Exchange identity table. |
| `GetMailTips` | Implemented, partial Exchange parity | Canonical directory plus Sieve vacation projection | Supports bounded invalid-recipient and OOF tips. |
| `GetServiceConfiguration` | Implemented, partial Exchange parity | Existing bounded EWS capability projection only | Returns MailTips service configuration for the implemented MailTips surface; requested Unified Messaging, Protection Rules, Policy Tips, and unknown configuration families return parseable EWS gaps instead of invented service state. |
| `GetUserRetentionPolicyTags` | Implemented, partial Exchange parity | `retention_policy_tags`, `account_retention_policy_assignments` | Projects active same-tenant visible tags plus the authenticated account's assigned default tag; this is the same visibility boundary used by `CreateManagedFolder`. |
| `GetDiscoverySearchConfiguration` | Implemented, partial Exchange parity | `discovery_searches` | Projects bounded same-tenant discovery search definitions; no Exchange search-mailbox policy or role model is exposed through EWS. |
| `GetSearchableMailboxes` | Implemented, partial Exchange parity | Same-tenant `accounts` | Projects same-tenant account mailboxes and litigation-hold flags; no Exchange discovery scope grants or external mailbox federation. |
| `SearchMailboxes` | Implemented, partial Exchange parity | `discovery_searches`, `discovery_search_jobs`, `discovery_result_items`, `mail_search_documents` | Creates canonical discovery search/job/result rows from Bcc-safe search documents; no Exchange preview/estimate/de-dup/refiner parity. |
| `FindMessageTrackingReport` | Implemented, partial Exchange parity | `submission_queue`, visible `submission_recipients`, canonical sent-message metadata | Finds authenticated-account outbound tracking reports from canonical LPE submission rows only; protected `Bcc` recipients are not projected and no Exchange tracking-report store is created. |
| `GetMessageTrackingReport` | Implemented, partial Exchange parity | `submission_queue`, `submission_events`, `lpe_ct_transport_trace_events` | Returns bounded same-account report detail and LPE/LPE-CT trace events by canonical queue id or trace id; cross-account and cross-tenant traces return not found. |
| `GetHoldOnMailboxes` | Implemented, partial Exchange parity | `compliance_holds`, `compliance_hold_mailboxes`, account litigation-hold flags | Returns canonical same-tenant hold state only. |
| `SetHoldOnMailboxes` | Implemented, partial Exchange parity | `compliance_holds`, `compliance_hold_mailboxes`, account litigation-hold flags, audit events | Creates or releases bounded canonical litigation holds; no Exchange In-Place Hold policy distribution semantics. |
| `GetNonIndexableItemDetails` | Implemented, partial Exchange parity | `non_indexable_item_reports` | Projects canonical search diagnostics without protected Bcc metadata. |
| `GetNonIndexableItemStatistics` | Implemented, partial Exchange parity | `non_indexable_item_reports` | Aggregates bounded report counts per mailbox; no Exchange crawl mailbox statistics. |
| `UploadItems` | Implemented, partial Exchange parity | `mailbox_item_transfer_jobs`, `mailbox_item_transfer_entries` | Records canonical EWS import jobs and entries; full Exchange streamed item import/MIME conversion remains bounded. |
| `ExportItems` | Implemented, partial Exchange parity | `mailbox_item_transfer_jobs`, `mailbox_item_transfer_entries` | Records canonical EWS export jobs and entries for requested item ids; full Exchange item byte-stream export remains bounded. |
| `GetSharingMetadata` | Implemented, partial Exchange parity | Owned canonical contact/calendar collections | Returns bounded sharing metadata for shareable contact/calendar collections only; no Exchange invitation tokens, federation metadata, mailbox-folder sharing, or external recipients. |
| `GetSharingFolder` | Implemented, partial Exchange parity | Accessible canonical contact/calendar collections and grants | Resolves same-tenant account owners and returns only already accessible shared contact/calendar folders; ungranted and cross-tenant folders return parseable EWS errors. |
| `RefreshSharingFolder` | Implemented, partial Exchange parity | Accessible canonical contact/calendar collections and grants | Verifies the shared contact/calendar folder remains accessible; no Exchange remote/federated sync state is refreshed. |
| `ExpandDL` | Implemented, partial Exchange parity | Same-tenant canonical directory/group projection | Expands visible public distribution-list entries from canonical membership emails; no recursive expansion or private Exchange DL item expansion. |
| `GetUserPhoto` | Implemented parseable canonical gap | Same-tenant canonical directory/account lookup | Validates same-tenant visible directory entries, then returns a parseable no-photo response because LPE has no canonical account/contact photo blob state. |
| `GetPasswordExpirationDate` | Implemented parseable canonical gap | Authenticated canonical account | Returns a parseable gap because LPE has no canonical password-expiration date field; rejects querying other accounts. |
| `MarkAsJunk` | Implemented, partial Exchange parity | Canonical mailbox message move to Junk | Supports `IsJunk=true` and `MoveItem=true` by moving canonical messages to the Junk mailbox; Exchange blocked-sender/unblock-only behavior returns parseable gaps instead of protocol-local junk state. |
| UCS IM operations | Implemented, partial Exchange parity | `contact_books` with `im_contact_list`, `contact_groups`, `contact_group_members`, same-tenant directory/group projection | Supports canonical IM group create/update/delete, contact/account/tel URI members, and visible distribution-list membership. No Exchange UCS-only store, presence, privacy relationship, or recursive DL expansion is created. |
| `GetRooms` | Implemented, partial Exchange parity | Tenant-visible room/equipment directory accounts | No full Exchange room-list object or cross-tenant GAL behavior. |
| `GetRoomLists` | Implemented, partial Exchange parity | Computed tenant room/resource list | Returns a computed list when visible room/equipment accounts exist; not arbitrary Exchange room-list membership. |
| `Subscribe` | Implemented, partial Exchange parity | Canonical `mail_change_log` cursor | Pull subscriptions return deterministic EWS ids/watermarks; no EWS subscription table is created. |
| `GetEvents` | Implemented, partial Exchange parity | Canonical notification replay over `mail_change_log` | No protocol-local notification queue or current-mailbox fallback; expired watermarks return parseable EWS errors. |
| `GetStreamingEvents` | Implemented, partial Exchange parity | Canonical notification replay over `mail_change_log` | Immediate bounded streaming response; no full Exchange long-held streaming affinity. |
| `Unsubscribe` | Implemented, partial Exchange parity | Compatibility response over cursor-based subscription truth | No persisted EWS subscription lifecycle. |
| `AddDelegate` | Implemented, partial Exchange parity | Canonical mailbox/calendar grants, sender rights, delegate preferences | Adds bounded Inbox/Calendar delegate rights and send-on-behalf only. |
| `GetDelegate` | Implemented, partial Exchange parity | Canonical delegate grants/preferences | Returns bounded delegate projection; no Exchange-only delegate folders. |
| `UpdateDelegate` | Implemented, partial Exchange parity | Canonical mailbox/calendar grants, sender rights, delegate preferences | Updates bounded canonical delegate rights only. |
| `RemoveDelegate` | Implemented, partial Exchange parity | Canonical grants, sender rights, delegate preferences | Removes canonical delegate state for same-tenant accounts. |
| `CreateUserConfiguration` | Implemented, partial Exchange parity | `account_client_configurations` | Stores bounded dictionary/XML/binary payloads. |
| `GetUserConfiguration` | Implemented, partial Exchange parity | `account_client_configurations` | Returns bounded dictionary/XML/binary payloads by account/folder/name scope. |
| `UpdateUserConfiguration` | Implemented, partial Exchange parity | `account_client_configurations` | Updates bounded user configuration blobs and advances canonical state. |
| `DeleteUserConfiguration` | Implemented, partial Exchange parity | `account_client_configurations` | Deletes canonical user configuration blobs. |
| `GetAppManifests` | Implemented, partial Exchange parity | `mail_app_catalog`, `mail_app_installations`, `mail_app_tenant_policies` | Returns same-tenant active catalog manifests visible to the authenticated account through install state or tenant default-install policy. |
| `GetAppMarketplaceUrl` | Implemented, partial Exchange parity | `mail_app_tenant_policies` | Returns only a configured canonical tenant marketplace URL; Exchange marketplace federation remains unsupported. |
| `InstallApp` | Implemented, partial Exchange parity | `mail_app_catalog`, `mail_app_tenant_policies`, `mail_app_installations`, `mail_app_consents` | Installs an approved active same-tenant catalog app for the authenticated account and grants the bounded `ews` consent scope. |
| `DisableApp` | Implemented, partial Exchange parity | `mail_app_installations` | Disables the authenticated account's canonical app installation. |
| `UninstallApp` | Implemented, partial Exchange parity | `mail_app_installations`, `mail_app_token_events` | Marks the authenticated account's canonical app installation uninstalled and revokes outstanding token events. |
| `GetClientAccessToken` | Implemented, partial Exchange parity | `mail_app_catalog`, `mail_app_installations`, `mail_app_consents`, `mail_app_token_events` | Issues an opaque bounded EWS add-in token and records only the canonical token hash/scope/expiry event. |
| `PlayOnPhone` | Implemented, partial Exchange parity | `unified_messaging_calls` | Records a canonical `play_on_phone` call request for the authenticated account; no real telephony control is performed by EWS. |
| `GetPhoneCallInformation` | Implemented, partial Exchange parity | `unified_messaging_calls` | Returns same-account canonical Unified Messaging call state. |
| `DisconnectPhoneCall` | Implemented, partial Exchange parity | `unified_messaging_calls` | Marks an active same-account canonical call as cancelled. |

## Explicitly Unsupported Dispatcher Operations

No Microsoft-documented operation name is currently left as an explicit unsupported-only dispatcher branch.

## Microsoft EWS Operations Without Canonical Behavior

No Microsoft-documented operation name is currently left without canonical EWS behavior in `LPE`. Future unsupported-only branches must remain explicit parseable unsupported responses until a documented canonical implementation exists.

| Microsoft area | Explicit unsupported operations |
| --- | --- |
| None | None |

## Visible Differences and Workarounds

Current EWS compatibility behavior differs visibly from Exchange in these ways:

- The adapter has two case-compatible EWS routes: `/EWS/Exchange.asmx` and `/ews/exchange.asmx`.
- EWS authentication uses the shared mailbox account authentication path and returns an EWS Basic challenge when needed.
- Operation detection is based on the first SOAP body operation element name; it is not full WSDL/XSD validation.
- Unknown or out-of-scope operation names return parseable EWS error payloads instead of raw transport failures.
- EWS ids are LPE canonical prefixed identifiers. `ConvertId` adds deterministic stateless opaque alternate ids instead of Exchange-generated identity rows.
- `FindFolder`, `GetFolder`, `SyncFolderHierarchy`, `FindItem`, `GetItem`, `SyncFolderItems`, `CreateItem`, `UpdateItem`, `DeleteItem`, `MoveItem`, `CopyItem`, `CreateFolder`, `CreateFolderPath`, `CopyFolder`, `EmptyFolder`, `UpdateFolder`, and `DeleteFolder` include bounded public-folder behavior over canonical public-folder tables.
- Public-folder item move is implemented as canonical clone into target folder plus delete of the original item.
- Public-folder folder move is explicitly rejected because canonical public-folder reparenting is not yet implemented.
- Public-folder item support is bounded to post-like items; arbitrary Exchange item classes and recipient-bearing public-folder conversions remain out of scope.
- `SyncFolderItems` uses synthetic sync-state strings over current visible state rather than durable Exchange sync cursors.
- Pull and bounded streaming notifications replay canonical `mail_change_log` state through shared notification polling; they are not held in EWS-local memory or SQL subscription rows.
- `GetEvents` returns status when no canonical change exists and parseable `ErrorInvalidWatermark` when the requested cursor has expired from retention.
- `GetStreamingEvents` is an immediate bounded response rather than a full long-held Exchange streaming subscription session.
- `GetServerTimeZones` is a small static compatibility response.
- OOF maps to canonical Sieve vacation scripts and LPE metadata comments, not an Exchange OOF table.
- `CreateAttachment` validates bytes with Magika before storage and accepts only `FileAttachment`.
- `CreateItem` send dispositions and `SendItem` use canonical LPE submission and authoritative Sent visibility; there is no EWS/client SMTP path.
- `SendItem` submits existing canonical draft ids, preserves protected `Bcc` in canonical submission/Sent state, and does not create EWS-local Outbox or Sent state.
- `GetInboxRules` projects canonical Sieve-backed mailbox rules only.
- `UpdateInboxRules` creates, updates, and deletes only bounded rules that map safely to canonical Sieve behavior. Exchange rule blobs, client-only rules, and unsupported deferred-action data return parseable EWS errors without Sieve side effects.
- `ArchiveItem` moves supported canonical message ids into the canonical `archive` mailbox role used by JMAP/IMAP/MAPI projections. It does not create an Exchange archive mailbox, online archive hierarchy, archive retention policy, or archive mailbox store.
- `GetReminders` returns computed canonical calendar/task/message reminder views.
- `PerformReminderAction` dismisses canonical calendar/task reminders through `reminder_occurrence_dismissals` and snoozes them by updating canonical reminder time/reset state; it does not create EWS reminder rows.
- `GetRooms` projects tenant-visible canonical room and equipment directory accounts only; hidden accounts, person accounts, and other tenants are excluded.
- `GetRoomLists` returns LPE's computed tenant room/resource list when visible room or equipment accounts exist. Arbitrary Exchange room-list membership is not implemented.
- UCS IM operations use canonical contact groups and IM-list state. IM groups are `contact_groups.group_kind = 'im_group'` under the authenticated account's `im_contact_list` contact book; members are `contact_group_members` rows for contacts, same-tenant accounts, tel URIs, and visible distribution lists. Cross-tenant distribution lists are rejected through the same address-book visibility path used by `ResolveNames` and `ExpandDL`.
- Persona operations are stateless projections over canonical same-tenant accounts and accessible contacts. `FindPeople` and `GetPersona` do not create linked-person, relevance, contact-merge, photo, or Exchange persona rows; `GetPersona` returns not found when the canonical account/contact is no longer visible to the authenticated principal.
- Delegate operations mutate canonical grants, sender rights, and delegate preferences. They do not create Exchange-only delegate folder state.
- User-configuration operations persist bounded payloads in `account_client_configurations`; there is no Exchange roaming-config side table.
- `GetMailTips` uses canonical directory visibility and Sieve vacation projection; it is not a full Exchange policy-tip/mailbox-quota/mail-flow tip engine.
- `GetServiceConfiguration` exposes only the existing bounded MailTips capability. Unified Messaging, Protection Rules, Policy Tips, and unknown service-configuration probes return per-configuration EWS `ErrorInvalidOperation` response messages because LPE does not currently have canonical engines for those Exchange service settings.
- `GetUserRetentionPolicyTags` returns only active same-tenant tags that are visible or assigned as the authenticated account's default tag. `OptedInto` is derived from `account_retention_policy_assignments.default_tag_id`. `CreateManagedFolder` uses the same same-tenant visible/assigned boundary and limits creation to canonical custom/personal retention-tag folders.
- eDiscovery operations route through canonical compliance/search tables. Search results come from `mail_search_documents`, which excludes protected `Bcc`; hold operations mutate canonical compliance hold rows and account litigation-hold flags; non-indexable responses project `non_indexable_item_reports`.
- Message tracking operations route through canonical `submission_queue`, `submission_events`, and `lpe_ct_transport_trace_events` rows. `FindMessageTrackingReport` lists only authenticated-account outbound submissions in the current tenant and includes visible recipients only. `GetMessageTrackingReport` returns bounded LPE and LPE-CT trace events by canonical queue id or trace id. No EWS-local tracking store is created, and cross-account or cross-tenant report lookups return not found/empty responses without leaking trace existence.
- `UploadItems` and `ExportItems` record canonical transfer jobs and entries for EWS import/export requests. They do not implement full Exchange item byte-stream conversion or protocol-local transfer storage.
- Mail app operations use only canonical tenant catalog/policy, account installation, consent, and token-event rows. They do not fetch remote Exchange marketplace content, accept arbitrary client-supplied manifests outside `mail_app_catalog`, or create protocol-local OAuth delegation state.
- Unified Messaging operations use only `unified_messaging_calls`. `PlayOnPhone` records a canonical call request, `GetPhoneCallInformation` reads same-account call state, and `DisconnectPhoneCall` cancels active canonical calls; EWS does not integrate with PBX/voicemail transport, dial plans, greetings, PINs, or Exchange UM mailbox policy behavior.
- Sharing operations are bounded to canonical same-tenant contact/calendar grants. `AcceptSharingInvitation` creates or updates a canonical grant for the authenticated account, `GetSharingFolder` and `RefreshSharingFolder` only expose already accessible shared contact/calendar collections, and `GetSharingMetadata` emits shareable owned collection metadata without Exchange tokens or federation state.
- Conversation operations use the existing lightweight `mailbox_messages.thread_id` value as bounded conversation identity. This is sufficient for same-account message grouping, item fetch, and one-shot read/move/delete actions over current canonical messages, but it is not full Exchange conversation identity: there is no first-class `threads` table, conversation lifecycle, recursive conversation index, historical deleted-item membership, or durable "always apply to future messages" rule created by EWS.
- `DeleteItem` maps Exchange delete types onto canonical hard delete or Trash move behavior.

## SQL Data Sufficiency

The current SQL schema contains the durable state needed for the implemented bounded EWS behavior:

| EWS capability | SQL support |
| --- | --- |
| Auth, tenant/domain/account identity | `tenants`, `domains`, `accounts`, `account_email_addresses`, `account_credentials`, `account_sessions`, app-password/OIDC tables |
| Mail folders/messages | `mailboxes`, `mailbox_messages`, `messages`, `message_headers`, `message_recipients`, `protected_bcc_recipients`, `message_bodies`, `mail_search_documents` |
| Attachments and MIME/blob metadata | `blobs`, `blob_placements`, `mime_parts`, `attachments`, `attachment_extraction_jobs`, `attachment_texts`, `calendar_event_attachments` |
| Contacts | `contact_books`, `contacts`, `contact_book_grants` |
| Contact groups / IM list projections | `contact_groups`, `contact_group_members`, `contact_books` with `im_contact_list` role |
| Calendar/free-busy | `calendars`, `calendar_events`, `calendar_grants` |
| Tasks/reminders | `task_lists`, `tasks`, `reminder_occurrence_dismissals` |
| OOF/vacation and mail tips | `sieve_scripts`, `sieve_vacation_responses`, canonical directory tables |
| Inbox rules | Sieve rule/script state plus EWS bounded rule projection code |
| Submission/Sent | `account_identities`, `sender_rights`, `submission_queue`, `submission_recipients`, `submission_events`, canonical mail tables |
| Message tracking bridge | `submission_queue`, `submission_events`, `lpe_ct_transport_trace_events`, `lpe_ct_inbound_delivery_receipts` |
| Public folders | `public_folder_trees`, `public_folders`, `public_folder_items`, `public_folder_permissions`, `public_folder_replicas`, `public_folder_per_user_state` |
| User configuration | `account_client_configurations` |
| Delegates | `delegate_preferences`, mailbox/calendar grants, `sender_rights` |
| Retention policy tags and managed retention folders | `retention_policy_tags`, `account_retention_policy_assignments`, `mailboxes.retention_policy_tag_id` |
| eDiscovery/compliance | `compliance_cases`, `compliance_holds`, `compliance_hold_mailboxes`, `discovery_searches`, `discovery_search_jobs`, `discovery_result_items`, `non_indexable_item_reports` |
| Bulk item transfer | `mailbox_item_transfer_jobs`, `mailbox_item_transfer_entries`, canonical item/blob tables |
| Mail apps | `mail_app_catalog`, `mail_app_tenant_policies`, `mail_app_installations`, `mail_app_consents`, `mail_app_token_events` |
| Unified Messaging | `unified_messaging_calls` |
| Cross-protocol changes/deletes | `account_sync_state`, `mail_change_log`, `tombstones`, `canonical_change_journal`, `recoverable_items` |
| Audit | `audit_events` |

The schema is adequate for the currently implemented bounded EWS surface, including mail apps and Unified Messaging. Remaining storage-ready gaps still need behavior and SOAP response work before they can be advertised as implemented.

Remaining full-parity limits are primarily behavior and protocol semantics, not raw SQL table absence:

- Exchange managed-folder policy blobs, online archive-mailbox, and full search-folder behavior are not fully modeled through EWS. `CreateManagedFolder` is limited to canonical mailbox folders linked to canonical retention tags.
- Push/streaming subscription affinity is bounded to canonical replay, not Exchange long-lived subscription semantics.
- Room lists are computed from tenant room/resource accounts, not stored as arbitrary Exchange distribution-room-list objects.
- Policy-tip operation handlers are not wired even where canonical schema exists.
- `ConvertId` is stateless and deterministic for supported LPE ids; it is not full Exchange id compatibility with every historical id format.

## Verification

The current EWS tests include coverage for folder operations, managed retention folder creation, `ArchiveItem`, `SendItem`, delegation, user configuration, inbox rules, reminders, rooms, sharing metadata/folder/refresh/invitation acceptance, eDiscovery search/hold/non-indexable reports, message tracking report find/detail over canonical LPE/LPE-CT trace rows with same-tenant isolation, persona find/detail over canonical account/contact rows with visibility checks, UCS IM group/list/member operations with distribution-list tenant isolation, bulk transfer job recording, mail app catalog/install/token state, Unified Messaging call state, `MarkAllItemsAsRead`, conversation listing/fetch/actions, `GetMailTips`, `GetServiceConfiguration`, `GetUserRetentionPolicyTags`, `ExpandDL`, `GetUserPhoto`, `GetPasswordExpirationDate`, `MarkAsJunk`, `ConvertId`, and notification replay paths. `ews_catalog_gate_covers_documented_operations_and_unsupported_gaps` owns a Microsoft EWS operation catalog snapshot, verifies that the parity matrix still matches it, requires every documented operation to name an existing SOAP test, and keeps unsupported-operation reasons explicit when unsupported-only branches exist.

Verification commands for this audit:

- `cargo test -p lpe-exchange --no-default-features create_managed_folder`: passed, 2 tests.
- `cargo test -p lpe-exchange --no-default-features ews_catalog_gate_covers_documented_operations_and_unsupported_gaps`: passed, 1 test.
- `cargo test -p lpe-exchange --no-default-features`: currently fails in unrelated MAPI-over-HTTP tests `mapi_over_http_save_message_replaces_out_of_range_import_source_key` and `mapi_over_http_sync_import_hard_delete_reports_partial_when_retention_blocks_delete`.
- `cargo test -p lpe-storage --test runtime_schema_drift`: passed, 1 test.

## Conclusions

1. The EWS audit result has changed materially: SOAP-tested top-level operation-name coverage is now 100.0%, and canonical behavioral operation-name coverage is 100.0%, up from the previous 32-operation snapshot.
2. The newly wired operations route through canonical LPE APIs/storage or stateless compatibility codecs. I did not find EWS-local Sent, Outbox, reminder, delegate, rule, user-configuration, or subscription state competing with canonical state.
3. SQL is sufficient for the bounded implemented EWS behavior and includes canonical state for several remaining parity gaps.
4. `LPE` is still not 100% Microsoft EWS compatible because many implemented operations, including `CreateManagedFolder`, have bounded canonical semantics instead of full Exchange server behavior.

## Highest-Value Remaining EWS Gaps

Priority for practical Outlook/native-client compatibility:

1. There is no remaining operation-name blocker to 100% behavioral coverage in the Microsoft EWS catalog snapshot.
2. Widen archive behavior beyond canonical Archive mailbox moves only if online/archive-mailbox lifecycle semantics are documented first; widen persona only if a canonical linked-person model is documented first.
3. Expand mail app and Unified Messaging behavior only when canonical marketplace, OAuth delegation, voicemail, dial-plan, or telephony APIs exist.
