# 0.2.0 Protocol Depth Gates

## Current State/Functionality Overview

The `0.2.0` protocol depth gates define the minimum release evidence for existing protocol surfaces before adding new protocol breadth. They bind JMAP, IMAP, ActiveSync, EWS, and MAPI over HTTP to canonical `LPE` state and client interoperability checks.

These gates do not move `SMTP` into core `LPE`, do not create protocol-local `Sent`, `Drafts`, or `Outbox` state, and do not widen the documented protocol scope.

## Implementation/Usage

Success criteria:

- each adapter uses canonical mailbox, contact, calendar, task, submission, and rights state where applicable
- every send path creates the authoritative `Sent` copy through core `LPE`
- `Bcc` remains protected from user search and normal shared-mailbox projections
- public autodiscover/autoconfiguration publishes only implemented and explicitly enabled endpoints
- local unit/integration tests pass for the relevant crates
- live harnesses pass before enabling public client metadata in a deployment

Required local verification:

```powershell
cargo test -p lpe-jmap
cargo test -p lpe-imap
cargo test -p lpe-activesync
cargo test -p lpe-exchange
```

Required live verification before public publication:

```powershell
perl tools/jmap_tester_big_three_check.pl
python tools/jmap_live_shared_delegated_check.py
python tools/ews_live_smoke_check.py
python tools/rca_outlook_connectivity_check.py --ews-readiness --allow-mutating-fixtures
python tools/rca_outlook_connectivity_check.py --outlook-rca-readiness --allow-mutating-fixtures
```

The live commands require a running `LPE` deployment, configured test accounts, and the environment variables documented by each tool. They are release/publication gates, not compile-time gates.

Current verification evidence:

| Date | Scope | Evidence |
| --- | --- | --- |
| 2026-05-08 | local protocol crates | `cargo fmt --check`; `cargo test -p lpe-jmap`; `cargo test -p lpe-imap`; `cargo test -p lpe-activesync`; `cargo test -p lpe-exchange`; `cargo test -p lpe-storage`; `cargo test --workspace` |
| 2026-05-08 | JMAP live state/change/push | `tools/jmap_tester_big_three_check.pl` and `tools/jmap_live_shared_delegated_check.py` passed against the configured live test deployment |
| 2026-05-08 | ActiveSync live publication | authenticated `OPTIONS /Microsoft-Server-ActiveSync` returned protocol version `16.1` and the implemented command set |
| 2026-05-08 | EWS live bounded matrix | `tools/ews_live_smoke_check.py --mutating` and `tools/rca_outlook_connectivity_check.py --ews-readiness --expect-exchange-providers --expect-mapi --allow-mutating-fixtures --expected-service-host mail.l-p-e.ch` passed against the configured live test deployment |
| 2026-05-08 | MAPI/RPC live Outlook readiness | `tools/rca_outlook_connectivity_check.py --outlook-rca-readiness --allow-mutating-fixtures --expected-service-host mail.l-p-e.ch` passed against the configured live test deployment |

## Reference Table/List

| Gate | Required evidence | Local coverage |
| --- | --- | --- |
| JMAP state/change/push consistency | `changes`, `queryChanges`, WebSocket push reconnect, journal replay, shared/delegated mailbox state, Bcc-safe projections | `crates/lpe-jmap` state, query, shared mailbox, and WebSocket push tests; `tools/jmap_tester_big_three_check.pl`; `tools/jmap_live_shared_delegated_check.py` |
| IMAP Outlook/Thunderbird compatibility transcripts | Outlook first-login/list/select/sync transcript, Thunderbird trash/expunge and draft deletion flows, UID stability, large-mailbox refresh, CONDSTORE, IDLE behavior | `crates/lpe-imap::tests::outlook_first_login_list_select_sync_transcript`, Thunderbird transcript tests, UID refresh, large-mailbox refresh, CONDSTORE, IDLE, quota probe tests |
| ActiveSync mobile/iOS compatibility lab | Outlook mobile and iOS Mail enrollment, `FolderSync`, `Sync`, send, smart reply/forward, attachments, `Ping`, long-poll reconnect, stale-key recovery | `crates/lpe-activesync` FolderSync, SyncKey, SendMail, SmartReply, SmartForward, ItemOperations, Search, Ping, long-poll reconnect, stale-key, contact/calendar mutation tests |
| EWS bounded operation matrix | documented supported and unsupported operation set; canonical mail/contact/calendar/task/submission behavior | `crates/lpe-exchange` EWS operation tests; `tools/ews_live_smoke_check.py`; EWS readiness mode in `tools/rca_outlook_connectivity_check.py` |
| MAPI over HTTP profile creation and mailbox sync | authenticated EMSMDB connect/logon/execute, no-event `NotificationWait`, idle EMSMDB/NSPI `PING` session refresh, NSPI bind/address-book rows, folder-scoped content sync, chunked FastTransfer continuation across `Execute` calls, reconnect `Connect`/`Bind` preserving valid session context, hierarchy sync, stable change-key/predecessor-list, read/flag-state, visible-recipient, and attachment-aware content change facts, canonical sync/import/read/delete/move, draft/send, `Sent`, repeated `Execute` request-id idempotency, parseable missing-header/malformed-header and disabled responses for unsupported NSPI mutation request types, RPC proxy readiness | `crates/lpe-exchange` MAPI/NSPI/RPC tests, including content/hierarchy manifest separation, `NotificationWait` cookie refresh, EMSMDB/NSPI `PING` cookie validation and expiration metadata, missing `X-RequestId` / `X-RequestType` / `X-ClientInfo` diagnostics, malformed `X-RequestId` / `X-ClientInfo` diagnostics, disabled `ModLinkAtt` / `ModProps` diagnostics, chunked FastTransfer continuation, reconnect handle/context preservation, stable change-key/predecessor-list, read/flag-state, visible-recipient, and attachment-aware sync manifests, and duplicate `Execute` replay without duplicate canonical submission; Outlook RCA readiness mode in `tools/rca_outlook_connectivity_check.py` |

| Publication decision | Gate |
| --- | --- |
| `JMAP` service discovery | JMAP session, API, upload/download, changes/queryChanges, and push gates pass |
| Thunderbird autoconfig | IMAP transcript gate passes and any published client `SMTP` endpoint is real `LPE-CT` authenticated submission |
| Outlook desktop IMAP path | IMAP Outlook transcript gate passes |
| ActiveSync mobile publication | ActiveSync mobile/iOS lab gate passes |
| EWS autodiscover | EWS bounded matrix passes and `LPE_AUTOCONFIG_EWS_ENABLED` is explicitly enabled |
| MAPI over HTTP autodiscover | MAPI profile/sync/reconnect gate passes and `LPE_AUTOCONFIG_MAPI_ENABLED` is explicitly enabled |
| legacy `EXCH` / `EXPR` metadata | Outlook RCA readiness passes; `EXPR` additionally requires exposed `/rpc/rpcproxy.dll` mailbox transport |
