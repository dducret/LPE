# Outlook Exchange Parity Roadmap

This roadmap consolidates Outlook and Exchange compatibility gaps from:

- `docs/audits/lpe-maintenance-outlook-architecture-audit-2026-06-27.md`
- `docs/architecture/ews-mapi-mvp.md`
- `docs/architecture/mapi-over-http-implementation-plan.md`
- `docs/architecture/ews-interoperability-matrix.md`
- `docs/audits/ews-parity-matrix-2026-05-30.md`

It is documentation-only. It does not change endpoint publication, runtime
behavior, schema, or release readiness.

## Scope Rules

- `LPE` remains the canonical store for mailbox, collaboration, submission,
  rights, search, recovery, and user-visible state.
- Outlook and Exchange compatibility metadata may be durable only when it
  supports protocol identity, profile reuse, associated configuration, or sync
  replay. It must not become mailbox truth.
- Public MAPI autodiscover remains gated by local harness evidence, Microsoft
  RCA evidence, and real Outlook 2016 / Outlook 2019 cached-mode profile
  evidence.
- A gap marked as not blocking public MAPI autodiscover can still block broader
  Exchange parity, EWS parity, native-client parity, or a later production
  hardening gate.

## Gap Tracker

| Protocol area | Current behavior | Why it matters to Outlook | Canonical model needed | Tests/evidence needed | Blocks public MAPI autodiscover? |
| --- | --- | --- | --- | --- | --- |
| MAPI publication and profile gate | MAPI endpoints exist but autodiscover publication is opt-in and requires the documented interop gate. Single-node sticky MAPI sessions are acceptable for the first lab gate. | Outlook desktop must create a real Exchange profile through autodiscover, select MAPI over HTTP, complete cached-mode sync, reopen, resolve NSPI, and send mail without manual endpoint overrides. | No new mailbox model; the gate depends on canonical mailbox/submission state plus durable Outlook metadata in `mapi_*` tables and session-only transport state. | Local harness pass, Microsoft RCA Outlook Connectivity pass, and separate Outlook 2016 and Outlook 2019 cached-mode evidence using `outlook-cached-mode-gate-evidence-template.md`. | Yes. This is the publication gate. |
| MAPI cross-process session replay and load-balanced failover | Session cookies, request sequencing, replay dedupe, handle tables, pending notifications, and transfer handles are session-local. Reconnect can use fresh `Connect` / `Bind` / `Logon`; cross-process replay is deferred. | Outlook cached mode can survive reconnects, but production deployments need predictable behavior when a worker restarts or load balancing moves the client. | Durable session replay is not required for the first gate. Production parity would need a canonical or durable transport-session replay model that does not store mailbox truth. | Existing transport/session tests plus restart and worker-migration tests proving either sticky-session requirements or durable replay behavior. | No for the first lab gate; yes for non-sticky production publication. |
| MAPI profile, table, and property projection breadth | Profile bootstrap, hierarchy/content tables, receive-folder tables, named properties, and bounded profile settings are implemented over canonical state. Property bags and table rows remain bounded to documented Outlook needs. | Outlook depends on stable FIDs/MIDs, source keys, change keys, named-property IDs, default-folder properties, table rows, and cached profile hints to keep an OST usable across reopen. | Canonical folder/message/contact/calendar/task state remains authoritative. Durable compatibility metadata stays in `mapi_object_identities`, `mapi_named_properties`, `mapi_profile_settings`, `mapi_folder_profile_property_values`, and related bounded tables. | Golden row/property tests, real Outlook profile open/reopen evidence, and cross-protocol checks proving MAPI property writes do not shadow canonical fields. | Yes if profile creation, hierarchy/content sync, or reopen fails; otherwise remaining property-bag breadth is parity debt. |
| MAPI FastTransfer and ICS parity | Content and hierarchy sync, checkpoint reuse, bounded FastTransfer copy/import, and upload state are implemented for canonical objects. Raw Exchange marker/subobject stream shapes and every property stream/conflict case are not complete. | Cached-mode Outlook relies on ICS to populate and incrementally maintain the OST. Incomplete stream support can cause sync stalls, duplicate items, or cache repair prompts. | Canonical change log, tombstones, membership state, object identities, and `mapi_sync_checkpoints`. Any widened upload stream must still commit through canonical item save/import paths. | ICS invariant tests, FastTransfer golden buffers, upload/import tests for every supported object family, and real Outlook cached-mode full-sync plus delta-sync evidence. | Yes for any missing behavior observed in profile sync or reopen. |
| MAPI send, draft, and transport/spooler advisory behavior | Draft save, submit, transport send, and bounded abort-submit route through canonical submission and authoritative `Sent`. `RopSetSpooler`, `RopSpoolerLockMessage`, and `RopTransportNewMail` are parsed but unsupported because there is no canonical client-spooler active flag, message lock, or client-announced new-mail model. | Outlook compose and send must converge with JMAP/IMAP-visible `Sent`. Spooler probes may appear during Outlook send/reconnect flows and must not corrupt transport custody. | Canonical submission queue, submission events, sender rights, drafts, protected Bcc storage, and optional future advisory state if Outlook traces prove it is required. LPE-CT remains transport custody owner after handoff. | End-to-end Outlook send tests, duplicate/replayed execute tests, Bcc-safe search/AI checks, canonical `Sent` checks, and trace-driven tests for advisory ROPs if implemented. | Yes for draft/send/`Sent`; advisory ROPs block only if real Outlook requires acknowledged advisory state. |
| MAPI NSPI and address-book mutation | NSPI bind, query, seek, resolve, URL discovery, distribution-list row projection, and visible account/contact lookup are implemented. `ModProps`, `ModLinkAtt`, distribution-list expansion/membership enumeration, and full address-book templates remain bounded or disabled. | Outlook uses NSPI during profile bootstrap, name resolution, address-book browsing, recipient resolution, and distribution-list workflows. | Canonical accounts, contacts, aliases/groups, grants, visibility rules, and possibly canonical group membership expansion. Mutations must go through contact/account APIs, not NSPI-local state. | NSPI bootstrap tests, ambiguous-name ranking tests, distribution-list expansion tests, tenant visibility tests, and real Outlook recipient-resolution evidence. | Yes for bind/resolve/profile bootstrap; mutation/template parity is broader Outlook functionality debt. |
| MAPI rules and deferred actions | `RopGetRulesTable` projects canonical Sieve-backed rules. `RopModifyRules` accepts only rule shapes that map to generated canonical Sieve. Exchange rule blobs, provider predicates, client-only rules, delegate rule templates, deferred-action provider data, and `RopUpdateDeferredActionMessages` are rejected. | Outlook exposes Inbox rules and may upload rule/deferred-action data. Incorrect acceptance could activate unsafe rules; rejection can limit Outlook rule management. | A canonical rule/deferred-action model that can represent supported Outlook rule semantics without storing opaque Exchange blobs as active behavior. Sieve remains the execution backend only for mappable rules. | Rule table projection tests, `RopModifyRules` mutation tests, EWS `GetInboxRules` / `UpdateInboxRules` parity tests, unsupported blob tests, and cross-protocol rule execution evidence. | No for profile publication unless Outlook setup requires these uploads; yes for full Outlook rule parity. |
| MAPI search folders and Common Views | Built-in and user-saved search folders project from canonical `search_folders`. `RopSetSearchCriteria` / `RopGetSearchCriteria` support a bounded JSON-mappable criteria subset. Full Microsoft search template BLOB parity, arbitrary restrictions, recipient/Bcc predicates, and secondary reminder promotion remain deferred. | Outlook relies on Search Folders, Common Views, reminders, and saved views for mailbox navigation and cached-mode folder trees. | Canonical search-folder definitions and evaluators that can safely represent Outlook criteria while preserving Bcc protection. Associated configuration rows remain Outlook metadata, not canonical messages. | Search-folder CRUD/projection tests, MAPI criteria round-trip tests, Bcc exclusion tests, Common Views FAI tests, and Outlook folder-tree/cache evidence. | Yes only if profile bootstrap or hierarchy sync depends on unsupported search/view rows; otherwise parity debt. |
| MAPI notifications and event delivery | `RopRegisterNotification` and `NotificationWait` support bounded content/hierarchy signaling from session events and canonical change-log replay. Registration handles and pending queues are session-local. Full Exchange payload parity, cross-process replay, and spooler advisory event semantics remain deferred. | Outlook uses notifications to avoid stale folder views and to converge after changes without full resync. | Canonical `mail_change_log` replay remains source of truth. Production parity may need durable notification subscription metadata that does not become mailbox truth. | NotificationWait content/hierarchy tests, restart/reconnect tests, full payload golden tests if widened, and Outlook idle/change observation evidence. | No for initial publication if cached-mode sync converges; yes if Outlook profile tests show stale or broken views. |
| MAPI public folders | Public-folder logon, hierarchy/content projection, post item CRUD, ACL read/write, per-user read state, owning servers, and ghost-state derivation are bounded over canonical public-folder state. Cross-server replication, recipient-bearing item conversion, arbitrary Exchange per-user binary blobs, and whole-folder purge remain deferred or bounded. | Outlook public-folder clients expect hierarchy, permissions, read state, replicas, posting, and sometimes legacy per-user streams and replication semantics. | Canonical public folders, public-folder items, grants, replicas, per-user state, tombstones, and any future canonical model for recipient-bearing public-folder items or Exchange-compatible per-user blobs. | Public-folder MAPI and EWS CRUD tests, permission mutation tests, per-user state tests, replica/ghost tests, and Outlook public-folder client evidence. | No for private mailbox MAPI autodiscover; yes only if public-folder support is part of the publication claim. |
| MAPI recovery, dumpster, and recursive purge | Recoverable Items Root, Deletions, Versions, and Purges are bounded virtual projections over canonical `recoverable_items`. Some moves/restores/purges are supported; full Exchange Deletions-to-Purges progression, copy-on-write Versions, `OpenSoftDeleted`, folder-local soft-deleted rows, and non-mailbox recursive purge are not complete. | Outlook recovery UI and mailbox cleanup paths may use dumpster folders, soft-delete views, and purge behavior. | Canonical recovery lifecycle with retention/legal-hold semantics, tombstones, restore/purge APIs, and optional first-class versioning if Exchange parity requires it. No MAPI-local dumpster store. | Recoverable folder browse/restore/purge tests, retention/legal-hold partial completion tests, JMAP/IMAP/MAPI tombstone convergence tests, and Outlook recovery UI evidence. | No for basic profile publication; yes if release scope includes Outlook recovery parity. |
| MAPI receive-folder mutation and folder lifecycle edges | `RopGetReceiveFolder` and receive-folder table projection exist. `RopSetReceiveFolder` is parsed but unsupported. Folder move/copy is bounded to user-created canonical mailbox folders; system, virtual, and public-folder cases are rejected. | Outlook may probe receive-folder routing and can attempt folder lifecycle operations during profile use or repair. | Canonical receive-folder routing only if LPE supports configurable delivery targets. Folder lifecycle must remain in canonical mailbox/public-folder models with change-log replay. | Receive-folder mutation tests if implemented, folder move/copy/delete sync tests, system-folder rejection tests, and Outlook trace evidence. | No unless real Outlook requires `RopSetReceiveFolder` during profile creation or cached-mode operation. |
| MAPI streams and raw object fidelity | Attachment/body streams and bounded public-folder post streams are implemented for supported canonical objects. `RopLockRegionStream` / `RopUnlockRegionStream` return parseable errors; raw Exchange marker/subobject destination streams are not complete. | Outlook may use stream ROPs for rich bodies, attachments, embedded items, and object imports. | Canonical blob/attachment/body storage plus item attachment or subobject models if required. Stream writes must commit through canonical object save paths. | Stream golden tests, attachment/body round-trip tests, item-attachment tests if added, and Outlook compose/attachment traces. | Yes if missing stream behavior breaks compose/sync; otherwise parity debt. |
| EWS mailbox item, folder, attachment, and sync breadth | All Microsoft EWS catalog operation names are routed in the current architecture matrix, but operations are marked partial against Microsoft parity. Item classes, property shapes, folders, archive, recoverable behavior, attachments, and sync tokens remain bounded to canonical LPE behavior. The older audit matrix lists 18 operation-name blockers, including `ArchiveItem`. | Outlook and native clients may use EWS for calendar/mail features, fallback operations, or add-ins; incomplete item/folder shape support can break non-MAPI Exchange clients. | Canonical item/folder APIs for every supported class, canonical archive semantics if adopted, canonical sync cursors, attachment validation/storage, and Bcc-safe projections. | EWS catalog gate, SOAP behavior tests per operation, item-class/property-shape matrix tests, sync cursor tests, and real EWS client evidence. | Usually no for MAPI autodiscover; yes only if the MAPI publication claim includes EWS-dependent Outlook workflows that fail without it. |
| EWS notifications | EWS `Subscribe`, `GetEvents`, `GetStreamingEvents`, and `Unsubscribe` are bounded over canonical `mail_change_log`; push notifications and full Exchange streaming affinity are not implemented. | Native clients use EWS notifications for mailbox freshness and long-running event delivery. | Canonical notification replay and optional durable subscription/affinity model. No Exchange-only subscription table unless it is explicitly compatibility metadata and not mailbox truth. | EWS notification replay tests, restart/expiry tests, long-poll/streaming tests, and client evidence for pull, streaming, and push behavior. | No for MAPI autodiscover unless Outlook desktop requires EWS notifications for the published profile. |
| EWS rules, reminders, rooms, mail tips, user configuration, and service configuration | These operation families exist as bounded canonical projections. Full Exchange rule blobs, reminder response shape, explicit room-list membership, quota/moderation/custom mail tips, arbitrary user-configuration behavior, and full service configuration payloads remain partial. | Outlook and EWS clients expose these as common mailbox UX: rules, reminders, room scheduling, compose tips, view/config blobs, and service capabilities. | Canonical Sieve-backed rules, reminder state, room/resource membership if needed, compose advisory state, bounded user/client configuration blobs, and tenant feature policy state. | SOAP tests per operation family, canonical storage tests, unsupported-shape tests, and real client workflow evidence. | No for basic MAPI publication; yes for a full Outlook/native-client parity claim covering these workflows. |
| EWS delegate, free/busy, sharing, and persona | Delegation and free/busy are bounded canonical projections. Sharing is same-tenant/contact/calendar scoped. Persona operations are partial or historically unsupported in older audit data; linked-person aggregation and Exchange-only delegate folders are absent. | Outlook scheduling, delegate mailbox/calendar workflows, people cards, and shared folders depend on these surfaces. | Canonical grants, sender rights, delegate preferences, free/busy computation, sharing invitations/grants, and optional persona aggregation/link state. | EWS delegate/free-busy/sharing/persona SOAP tests, MAPI convergence tests for permissions/delegation, tenant isolation tests, and Outlook scheduling/delegate evidence. | No for basic MAPI profile publication unless delegate/shared-calendar scenarios are in the gate; yes for delegated Outlook parity. |
| EWS bulk transfer, message tracking, eDiscovery, mail apps, UM, UCS/IM, retention, time zones | These are lower-priority or feature-family-specific parity areas. Some are bounded partial; older audit data lists message tracking, UCS/IM operations, and managed-folder behavior as missing/unsupported. | They matter to Exchange ecosystem clients, administrative tools, add-ins, compliance workflows, room/resource scheduling, or legacy features, but are not central to the first Outlook MAPI profile gate. | Canonical import/export jobs, LPE/LPE-CT trace projections, compliance search/hold state, add-in catalog/token state, UM call state if in scope, UCS/IM contacts/groups if product scope changes, retention tags, and a complete time-zone catalog if required. | EWS catalog gate coverage, SOAP tests, admin/security tests, LPE-CT boundary tests for tracking, and client/tool evidence for each feature family. | No for public MAPI autodiscover unless explicitly included in the published compatibility claim. |
| Outlook Anywhere / RPC over HTTP legacy shim | `/rpc/rpcproxy.dll` exists as a later legacy compatibility path, but `EXPR` publication is separately gated and must match real RPC/HTTP mailbox transport behavior. MAPI over HTTP is the first Outlook desktop path. | Older Outlook/RCA paths may probe `EXPR`; publishing it prematurely would advertise a transport that is not complete. | Same canonical EMSMDB behavior as MAPI over HTTP plus any required RPC/HTTP transport/session compatibility. No separate mailbox state. | RPC/HTTP transport tests, Microsoft RCA evidence for Outlook Anywhere, real legacy Outlook profile evidence, and explicit `EXPR` gate records. | No for MAPI over HTTP publication; yes for `EXPR` publication. |
| Autodiscover and endpoint publication evidence | EWS, MAPI HTTP, legacy `EXCH`, and legacy `EXPR` publication are all independently gated by environment flags and readiness evidence. Autodiscover must not publish endpoints that are not implemented and exposed. | Outlook profile creation is driven by autodiscover. Incorrect metadata can force the wrong protocol, advertise SMTP from core LPE, or make Outlook select incomplete legacy transports. | Canonical endpoint configuration, deployment host/TLS state, and documented readiness gates. No protocol-local endpoint state. | Autodiscover unit tests, Microsoft RCA evidence, real Outlook profile creation evidence, and deployment records showing exact flags and hostnames. | Yes. Mispublication directly blocks or invalidates public MAPI autodiscover. |

## Priority Interpretation

- `Yes` in the final column means the gap can block public MAPI autodiscover
  publication for the first Outlook desktop Exchange-account path.
- `No` means the gap is still tracked, but it belongs to broader EWS, Exchange,
  native-client, production-hardening, or feature-family parity.
- Some rows are conditional because the source docs intentionally separate the
  first Outlook 2016 / 2019 cached-mode lab gate from later production or
  legacy compatibility gates.

## Verification Expectations

Before a gap can be closed, update the relevant source matrix or architecture
document and add the evidence named in its row. At minimum:

- MAPI closure needs local `lpe-exchange` protocol tests plus real Outlook
  profile evidence when it affects profile creation, sync, send, reconnect, or
  autodiscover.
- EWS closure needs the EWS catalog gate to name SOAP behavior tests or an
  explicit unsupported reason for every affected Microsoft operation.
- Canonical-state closure needs cross-protocol tests proving JMAP, IMAP where
  applicable, EWS, and MAPI observe the same user-visible state.
- Any behavior that cannot map to current canonical state must first add or
  document a canonical model. Opaque Exchange blobs must not be accepted as
  active behavior by default.
