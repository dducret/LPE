# EWS Interoperability Matrix

## Current State/Functionality Overview

This matrix compares `LPE` against Microsoft's EWS operation catalog:

- <https://learn.microsoft.com/en-us/exchange/client-developer/web-service-reference/ews-operations-in-exchange>
- <https://learn.microsoft.com/en-us/exchange/client-developer/web-service-reference/ews-xml-elements-in-exchange>

`LPE` does not implement complete Microsoft Exchange Web Services. The current `lpe-exchange` EWS adapter is bounded compatibility over canonical `LPE` mailbox, contacts, calendar, task, public-folder, attachment, submission, OOF, availability, and notification change-log state.

Status meanings:

- `implemented`: complete enough for the Microsoft operation contract, with no known LPE-specific behavioral gap.
- `partial`: dispatched and useful, but bounded compared with Exchange.
- `unsupported`: explicitly recognized by the dispatcher and returned as a parseable EWS unsupported response.
- `missing`: listed by Microsoft but not implemented as an LPE EWS operation.

No current EWS operation is marked `implemented` against full Microsoft parity. Supported LPE operations are marked `partial` because they intentionally expose bounded canonical behavior rather than Exchange's full storage, identity, policy, and sync model.

## Implementation/Usage

- EWS routes are `POST /EWS/Exchange.asmx` and `POST /ews/exchange.asmx`.
- Authentication uses canonical mailbox account authentication.
- Supported writes must mutate canonical LPE state only.
- Message submission must use canonical submission and authoritative `Sent`; EWS must not introduce client `SMTP`, an EWS `Outbox`, or an EWS-only `Sent` copy.
- Unsupported operations must return parseable SOAP/EWS errors rather than generic transport failures.
- EWS ids are currently LPE-prefixed canonical ids such as `message:{uuid}`, `mailbox:{uuid}`, `contact:{uuid}`, `event:{uuid}`, `task:{uuid}`, `attachment:{message_uuid}:{attachment_uuid}`, `public-folder:{uuid}`, and `public-folder-item:{uuid}`. `ConvertId` also exposes a stateless opaque form for alternate EWS formats.
- EWS item `ChangeKey` values are opaque SHA-256 tokens over a canonical item id and its durable revision: mailbox-membership `modseq` for messages, `modseq` for contacts, calendar events, and tasks, and `change_counter` for public-folder items. Reads never advance a revision, so an unchanged item has the same key in `CreateItem`, `GetItem`, `FindItem`, `SyncFolderItems`, conversation results, and current-item notifications. Public-folder per-user read state keeps that shared item key stable and is returned through `SyncFolderItems` `ReadFlagChange`. This follows [MS-OXWSCORE] section 2.2.4.25, [MS-OXWSMSG] section 3.1.4.4, and [MS-OXWSSYNC] sections 3.1.4.2.3.2 and 3.1.4.2.3.5.

## Full Microsoft EWS Operation Matrix

For `partial` and `missing` rows, the planning columns identify the canonical LPE API, SQL state, tests, docs, and client-visible behavior required to close the parity gap. `N/A` means the operation is intentionally unsupported and should remain a parseable unsupported response unless product scope changes.

| Area | Operation | Status | Required canonical LPE API | Required SQL state | Required tests | Required docs | Client-visible behavior |
| --- | --- | --- | --- | --- | --- | --- | --- |
| eDiscovery | `GetDiscoverySearchConfiguration` | partial | Existing compliance discovery API for saved searches and hold-scoped sources | compliance_cases, discovery_searches, compliance_holds, audit_events | EWS config read, tenant scoping, permission denial | eDiscovery architecture and admin docs | Authorized compliance clients see discovery searches; normal users get EWS access errors |
| eDiscovery | `GetHoldOnMailboxes` | partial | Existing compliance hold API | compliance_holds, compliance_hold_mailboxes, audit_events | get hold state, cross-tenant denial | data lifecycle and eDiscovery docs | Reports mailbox hold state only to authorized roles |
| eDiscovery | `GetNonIndexableItemDetails` | partial | Existing search diagnostics API | non_indexable_item_reports linked to messages and attachments | non-indexable detail pagination, Bcc protection | search and compliance docs | Returns diagnostic item details without exposing protected `Bcc` |
| eDiscovery | `GetNonIndexableItemStatistics` | partial | Existing search diagnostics API | non_indexable_item_reports aggregates per mailbox/search | statistics aggregation tests | search and compliance docs | Returns mailbox statistics for discovery tooling |
| eDiscovery | `GetSearchableMailboxes` | partial | Existing compliance mailbox discovery API | searchable mailbox projection over accounts/domains/grants | role filter, tenant isolation | eDiscovery admin docs | Lists only authorized searchable mailboxes |
| eDiscovery | `SearchMailboxes` | partial | Existing compliance search API over canonical mail search | discovery_searches, discovery_search_jobs, discovery_result_items, mail_search_documents, audit_events | query execution, export safety, protected metadata exclusion | search and compliance docs | Produces scoped discovery results without user-facing AI or `Bcc` leakage |
| eDiscovery | `SetHoldOnMailboxes` | partial | Existing compliance hold mutation API | compliance_holds, compliance_hold_mailboxes, message/recoverable legal_hold fields, audit_events | add/remove hold, idempotency, permission denial | data lifecycle and install/update docs if schema changes | Applies legal hold through canonical lifecycle state |
| Mailbox item | `ArchiveItem` | partial | Existing canonical mailbox move API scoped to the `archive` role | mailboxes, mailbox memberships, change log | archive move, sync replay, missing archive mailbox tests | JMAP/IMAP/MAPI mailbox role docs; online archive lifecycle docs if widened | Moves canonical message ids to LPE's Archive mailbox; not Exchange online/archive-mailbox semantics |
| Mailbox item | `CreateItem` | partial | Existing contact/event/task/mail/public-folder create plus full item-class mapper | messages, contacts, calendar_events, tasks, public_folder_items, attachments, submission_queue | item-class matrix, send dispositions, recurrence, attachment, Bcc-safe submission | EWS item-class mapping docs | Creates only canonical supported items today; an explicit `SavedItemFolderId` is strictly parsed before any write and unsupported item classes fail |
| Mailbox item | `CopyItem` | partial | Existing copy API plus full item class and folder coverage | mailbox_messages, messages, public_folder_items, change log | cross-folder copy, source/target membership, public/private guards, attachment preservation | EWS mailbox operation docs | Copies at most 100 supported messages or public-folder posts after complete target/source/access preflight in one canonical transaction |
| Mailbox item | `DeleteItem` | partial | Existing delete API plus complete delete-type/recoverable behavior | mailbox_messages, tombstones, recoverable_items, public_folder_items, change log | soft-delete rejection, hard, move-to-trash, recoverable and public-folder delete tests | delete/recovery docs | Deletes exactly one canonical target per request until a cross-family atomic delete transaction exists; `SoftDelete` is rejected until EWS projects canonical recoverable items, while supported delete types map to canonical Trash, hard delete, or public-folder deletion |
| Mailbox item | `FindItem` | partial | Existing item query API plus Exchange views, restrictions, sorts, all item classes | messages/search, contacts, calendar_events, tasks, public_folder_items | restriction/sort/view tests, Bcc exclusion | EWS query docs | Finds bounded canonical mail, contacts, events, tasks, and public-folder posts |
| Mailbox item | `GetItem` | partial | Existing item read API plus full property-shape and item-class mapper | canonical item tables, MIME, attachments, custom properties if added | property shape, body, MIME, attachment, unsupported id tests | EWS property mapping docs | Reads LPE-prefixed canonical ids only |
| Mailbox item | `MarkAllItemsAsRead` | partial | Existing mailbox-scoped canonical bulk read-state API | mailbox_messages read state, mail_change_log | bulk read/unread, exact folder membership scoping, bound rejection, public-folder unsupported-shape test | mailbox read-state docs | Marks at most 10,000 changed visible canonical mailbox messages in one supported folder read or unread |
| Mailbox item | `MoveItem` | partial | Existing move API plus full item-class and recoverable/public-folder semantics | mailbox_messages, tombstones, public_folder_items, change log | move, source tombstone/target create replay, copy-delete public-folder behavior | EWS mailbox operation docs | Moves at most 100 canonical messages or bounded public-folder posts after complete preflight in one canonical transaction |
| Mailbox item | `SendItem` | partial | Existing draft submit API over canonical draft ids; full Exchange saved-item options remain bounded | messages, mailbox_messages, submission_queue, submission_recipients, Sent copy | send saved draft, Bcc protection, Sent visibility, retry/idempotency | submission docs and EWS docs | Sends exactly one existing canonical draft through LPE submission until atomic batch submission exists; no EWS `Outbox` or EWS-only `Sent` state is created |
| Mailbox item | `UpdateItem` | partial | Existing update API plus full property and item-class mutation | canonical item tables, custom property table if widened, change log | property mutation matrix, recurrence, categories, read/flag tests | EWS property/update docs | Validates and updates exactly one bounded contact, event, task, message read/flag, or public-folder post mutation per request until a cross-family atomic batch transaction exists; contact and calendar mutations require a current `ChangeKey` |
| Folder | `CreateFolder` | partial | Existing mailbox/public-folder create plus full folder-class support | mailboxes, public_folders, change log | protected folder, public-folder ACL, duplicate name tests | folder mapping docs | Creates custom mail folders and child public folders only |
| Folder | `CreateFolderPath` | partial | Existing bounded path creation over mailbox/public-folder parents; atomic rollback remains future parity work | mailboxes/public_folders with parent chain, change log | nested creation, duplicate segment reuse, sync/change tests | folder hierarchy docs | Creates or reuses canonical path segments for supported mailbox and public-folder parents |
| Folder | `CreateManagedFolder` | partial | Canonical managed retention folder API over mailbox creation and retention tags | mailboxes.retention_policy_tag_id, retention_policy_tags, account_retention_policy_assignments, mail_change_log | SOAP create success, unavailable tag rejection, storage/API tag FK and tenant isolation tests | retention and EWS docs | Creates or reuses a canonical root custom mailbox folder for an active visible or assigned same-tenant custom/personal retention tag; no Exchange managed-folder policy blob is created |
| Folder | `CopyFolder` | partial | Existing bounded recursive copy over custom mailboxes and public-folder APIs | mailboxes, message memberships, public_folders, public_folder_items, change log | recursive copy, system-folder rejection, sync/change tests | folder operation docs | Copies supported custom mailbox/public-folder trees through canonical create/copy APIs |
| Folder | `DeleteFolder` | partial | Existing delete API plus complete recoverable/system semantics | mailboxes, public_folders, tombstones, recoverable_items, change log | protected folder, child handling, public-folder permissions | delete/recovery docs | Deletes only canonical deletable mailbox or public folders |
| Folder | `EmptyFolder` | partial | Existing bounded empty over canonical mailbox scoped delete and public-folder item delete APIs | mailbox_messages, tombstones, recoverable_items, public_folder_items, change log | mailbox/public-folder empty, protected/mixed rejection-before-mutation, DeleteSubFolders, sync/change tests | recovery lifecycle docs | Empties at most 10,000 items in one supported custom mailbox or public folder atomically; optional custom subfolders use the same canonical transaction |
| Folder | `FindFolder` | partial | Existing folder discovery plus full distinguished/archive/search/public folder projection | mailboxes, search_folders, public_folders | shape, paging, search-folder, archive tests | folder hierarchy docs | Projects canonical folders and bounded public-folder roots |
| Folder | `GetFolder` | partial | Existing folder read plus full property-shape mapping | mailboxes, public_folders, search_folders | distinguished id, shape, permission, multi-target ordering, and malformed-wrapper tests | folder property docs | Reads one nonempty `FolderIds` sequence of selected root, distinguished, mailbox, public-folder, and accessible collaboration ids in request order; unsupported or inaccessible targets fail without an unscoped projection |
| Folder | `MoveFolder` | partial | Existing bounded mailbox reparenting through canonical mailbox update; public-folder move remains explicitly rejected until canonical reparenting exists | mailboxes parent fields, change log | mailbox move, system-folder rejection, public-folder unsupported-shape test, sync/change tests | folder hierarchy docs | Moves supported custom mailbox folders only |
| Folder | `UpdateFolder` | partial | Existing bounded DisplayName update over canonical mailbox/public-folder update APIs | mailboxes, public_folders, change log | rename, protected/unsupported-shape rejection before mutation, sync/change tests | folder property docs | Updates one supported canonical folder DisplayName only |
| Attachment | `CreateAttachment` | partial | Canonical validated message-attachment create | blobs, blob_placements, attachments, mime_parts | Magika validation, one file attachment, item/reference/batch rejection | attachment docs and [MS-OXWSATT] §§2.2.4.5, 3.1.4.1 | Adds one validated file attachment to one canonical message parent |
| Attachment | `GetAttachment` | partial | Canonical attachment fetch and MIME/blob reconstruction | blobs, blob_placements, attachments, mime_parts, calendar_event_attachments | content/metadata, permission, missing blob, and shared-calendar authorization tests | attachment docs and [MS-OXWSATT] §§2.2.4.2, 3.1.4.3 | Fetches one bounded canonical message or calendar attachment; calendar attachment access requires effective event read rights and fetches from the event owner's canonical attachment storage |
| Attachment | `DeleteAttachment` | partial | Canonical attachment delete and parent version change | attachments, mime_parts, calendar_event_attachments, blob retention metadata, change log | delete, retained shared blob, parent kind, and shared-calendar authorization tests | attachment docs and [MS-OXWSATT] §§2.2.4.2, 3.1.4.2 | Deletes one bounded canonical message or calendar attachment; calendar attachment deletion requires effective event delete rights and updates the canonical event parent |
| Reminder | `GetReminders` | partial | Existing computed reminders API; full Exchange windowing and response-shape parity remain bounded | reminder_occurrence_dismissals plus calendar_events, tasks, and message follow-up reminder fields | due window, one-occurrence dismissal/snooze, cancelled/completed exclusion | reminders docs | Returns canonical calendar, task, and message follow-up reminders |
| Reminder | `PerformReminderAction` | partial | Existing reminder dismissal and per-occurrence snooze APIs; full Exchange reminder action parity remains bounded | reminder_occurrence_dismissals, task/event reminder metadata, mailbox message follow-up reminder dismissal | dismiss calendar/task reminders, per-occurrence snooze persistence, unsupported action rejection | reminders docs | Supports exactly one canonical dismiss or per-occurrence calendar/task snooze action per request until a canonical reminder-batch transaction exists |
| Conversation | `ApplyConversationAction` | partial | Existing bounded conversation action API | lightweight message-level thread ids, messages, mail_change_log | move/delete/read/category action tests | conversation/thread docs | Applies supported conversation-wide actions over canonical messages; richer Exchange conversation identity remains bounded |
| Conversation | `FindConversation` | partial | Existing conversation query API | message thread identifiers, search summaries | grouping, paging, folder scope tests | conversation docs | Lists conversations derived from canonical messages without a first-class Exchange conversation store |
| Conversation | `GetConversationItems` | partial | Existing conversation item expansion API | messages, mailbox_messages, thread state | expansion, Bcc-safe participants, permissions | conversation docs | Returns items in a canonical conversation while preserving Bcc protection |
| Utilities | `ConvertId` | partial | Existing stateless canonical EWS id codec | no SQL; opaque ids encode canonical family/id payloads | message, folder, contact, event, task, attachment, public-folder round trips; invalid id tests | identity mapping docs | Converts supported LPE EWS ids to/from deterministic opaque alternate ids without adding an Exchange identity table |
| Utilities | `ExpandDL` | partial | Existing address-book group expansion API | canonical directory/contact group projection | distribution-list expansion and tenant visibility tests | address book docs | Expands visible canonical distribution lists without adding an Exchange-only DL store |
| Utilities | `GetUserPhoto` | partial | Existing profile photo API | account profile photo/blob metadata | size, permission, absent photo tests | identity/profile docs | Returns mailbox/contact photos when canonical profile photos exist |
| Utilities | `MarkAsJunk` | partial | Existing bounded junk classification API | mailbox move state plus perimeter feedback/audit if scoped | junk/not-junk, LPE-CT boundary tests | mail security and EWS docs | Moves mail and records feedback without moving SMTP filtering into LPE core |
| Utilities | `ResolveNames` | partial | Existing address-book/contact resolution plus full GAL/persona templates | accounts, contacts, canonical group aliases, grants | invalid/ambiguous query, account/contact/distribution-list, hidden-entry, and Contacts-scope tests | address book docs | `ActiveDirectory` resolves one authenticated-account, tenant-directory account, accessible-contact, or canonical distribution-list match as `PublicDL`; `Contacts` excludes distribution lists, and ambiguous names are redacted errors |
| Utilities | `GetPasswordExpirationDate` | partial | Existing account credential policy API | password policy and credential expiry metadata | local password, external auth, no-expiry tests | auth/admin docs | Returns password expiry only for credential types with canonical expiry |
| Availability | `GetUserAvailability` | partial | Existing free/busy API plus attendee scope and richer suggestions | calendar_events, calendar_grants, delegation/free-busy state | free/busy visibility, cross-tenant denial, recurrence/time-zone window, suggestion tests | calendar availability docs | Returns bounded busy intervals for one readable canonical calendar owner without event details |
| Availability | `GetRoomLists` | partial | Existing directory projection; explicit room-list membership remains unsupported until canonical room-list state exists | accounts.directory_kind; computed tenant room/resource list projection only | room-list projection, empty directory tests, no Exchange-only room-list rows | EWS availability docs | Returns a computed tenant room/resource list when canonical room or equipment accounts exist |
| Availability | `GetRooms` | partial | Existing room/equipment directory projection; arbitrary room-list membership filtering is rejected | account directory kind, tenant scoping, GAL visibility | tenant scoping, hidden account, room/equipment/person filtering tests | room/equipment docs | Lists tenant-visible room and equipment accounts from canonical directory state |
| Availability | `GetUserOofSettings` | partial | Existing vacation settings projection | sieve_scripts, sieve_vacation_responses | disabled/enabled/scheduled round-trip tests | OOF/Sieve docs | Projects canonical Sieve vacation as EWS OOF |
| Availability | `SetUserOofSettings` | partial | Existing locked vacation settings mutation | sieve_scripts, sieve_vacation_responses, audit | scheduled/external audience/idempotency tests | OOF/Sieve docs | Atomically compare-and-replaces only the expected marker-bearing EWS-owned active vacation script; other active canonical Sieve state is preserved |
| Bulk transfer | `UploadItems` | partial | Existing bounded transfer-job API over explicit canonical item/source ids; arbitrary Exchange item packages remain rejected | mailbox_item_transfer_jobs, mailbox_item_transfer_entries, import audit | explicit id job, malformed package rejection, idempotency tests | import/export docs | Records bounded transfer intent for explicit canonical ids and rejects arbitrary package payloads without side effects |
| Bulk transfer | `ExportItems` | partial | Existing bulk export API over canonical items | mailbox_item_transfer_jobs, mailbox_item_transfer_entries, messages, MIME/blob reconstruction, contacts/events/tasks if scoped | export fidelity, attachment reconstruction, Bcc protection | import/export docs | Exports supported canonical items without leaking protected metadata |
| Delegate | `AddDelegate` | partial | Existing bounded delegation mutation API | mailbox_delegation_grants, calendar_grants, sender_rights, delegate_preferences, audit | add delegate, send-on-behalf, permission denial, unsupported Exchange-only shapes | delegation docs | Adds bounded Inbox/Calendar delegate rights and send-on-behalf through canonical state only |
| Delegate | `GetDelegate` | partial | Existing bounded delegation read API | mailbox_delegation_grants, calendar_grants, sender_rights, delegate_preferences | read delegate, same-tenant filtering | delegation docs | Returns bounded Inbox/Calendar delegate projection; no Exchange-only delegate folders |
| Delegate | `UpdateDelegate` | partial | Existing bounded delegation mutation API | mailbox_delegation_grants, calendar_grants, sender_rights, delegate_preferences, audit | update delegate rights, convergence with MAPI | delegation docs | Updates bounded canonical delegate rights only |
| Delegate | `RemoveDelegate` | partial | Existing bounded delegation mutation API | mailbox_delegation_grants, calendar_grants, sender_rights, delegate_preferences, audit | remove delegate, stale access denial | delegation docs | Removes canonical delegate grants, sender rights, and preferences |
| Inbox rules | `GetInboxRules` | partial | Exact generated active-Sieve rule-set projection; Exchange rule blobs/client-only rules remain unsupported | sieve_scripts | generated projection, tenant/account scope, arbitrary-Sieve suppression | Sieve/rules docs; [MS-OXWSRULES] §§3.1.4.1, 3.1.4.2 | Returns only the authenticated mailbox's exact reversible generated Sieve rules |
| Inbox rules | `UpdateInboxRules` | partial | One canonical generated-Sieve replacement transaction for bounded subject/fileinto and discard rules | sieve_scripts, change log, audit | create/update/delete, malformed batch rollback, conflict rejection | Sieve/rules docs; [MS-OXWSRULES] §§2.2.4.1, 2.2.4.4-.5, 3.1.4.2 | Mutates one ordered active generated script atomically; active unmanaged Sieve/OOF is preserved and blocks the adapter |
| Mail app | `DisableApp` | partial | Existing bounded account app-installation mutation API | mail_app_installations | disable app, tenant/user scope tests | mail app docs | Disables the authenticated account's canonical app install only |
| Mail app | `GetAppManifests` | partial | Existing bounded add-in catalog projection API | mail_app_catalog, mail_app_tenant_policies, mail_app_installations | manifest listing and policy tests | mail app docs | Returns same-tenant stored catalog manifests visible through install or policy state |
| Mail app | `GetAppMarketplaceUrl` | partial | Existing tenant marketplace policy API | mail_app_tenant_policies | configured URL and disabled tests | mail app docs | Returns configured canonical tenant URL or parseable Exchange marketplace gap |
| Mail app | `GetClientAccessToken` | partial | Existing bounded add-in token-event API | mail_app_catalog, mail_app_installations, mail_app_consents, mail_app_token_events | token scope, expiry, denial tests | mail app/security docs | Issues opaque bounded app tokens and stores only canonical token events |
| Mail app | `InstallApp` | partial | Existing bounded account add-in install API | mail_app_catalog, mail_app_tenant_policies, mail_app_installations, mail_app_consents | install, duplicate, policy denial tests | mail app docs | Installs an approved same-tenant catalog app for the authenticated account |
| Mail app | `UninstallApp` | partial | Existing bounded account add-in uninstall API | mail_app_installations, mail_app_token_events | uninstall and stale token tests | mail app docs | Marks the account install uninstalled and revokes token events |
| Mail tips | `GetMailTips` | partial | Existing address-book and canonical Sieve vacation projection APIs | accounts, contacts, groups, sieve_scripts | OOF, invalid recipient, same-tenant filtering tests | mail tips docs | Returns bounded invalid-recipient and OOF tips from canonical directory and vacation state |
| Message tracking | `FindMessageTrackingReport` | partial | Existing traceability API bridged to LPE-CT | submission_queue, submission_events, lpe_ct_transport_trace_events | submitted message lookup, authz, CT boundary tests | LPE-CT traceability docs | Finds tracking reports without making LPE core own SMTP perimeter state |
| Message tracking | `GetMessageTrackingReport` | partial | Existing traceability API bridged to LPE-CT | submission_events plus lpe_ct_transport_trace_events | delivery detail, DSN, permission tests | LPE-CT traceability docs | Shows delivery trace from canonical submission and CT relay state |
| Notification | `GetEvents` | partial | Pull replay over retained canonical `mail_change_log` rows with deterministic account-, folder-, event-, and cursor-bound tokens | mail_change_log; no Exchange subscription table or protocol-local event queue | restart, page-boundary, expiry, isolation, folder/event-filter tests | MS-OXWSNTIF §§2.2.4.1-.8, 2.2.5.1-.2, 3.1.4.1 | Returns retained mailbox-message changes once in cursor order; empty replay advances only to a durable snapshot fence and never synthesizes current-state events |
| Notification | `GetStreamingEvents` | partial | One immediate streaming-shaped replay for one pull token, using the same durable scope and retention checks | mail_change_log; no Exchange subscription table | stream request, expiry, scope/filter tests | MS-OXWSNTIF §§3.1.4.2, 3.1.4.2.3.1 | Does not hold a connection, create affinity, or implement push |
| Notification | `Subscribe` | partial | Pull-only token issuance seeded from a canonical replay cursor; push and resume-on-subscribe are unsupported | mail_change_log cursor; deterministic stateless subscription id/watermark only | pull shape, scope, invalid token, tenant tests | MS-OXWSNTIF §§3.1.4.3, 3.1.4.3.3.3-.5 | Accepts only authenticated mailbox folders and four canonical mailbox-message event types; stores no protocol-local mailbox truth |
| Notification | `Unsubscribe` | partial | Validated token compatibility response | no protocol-local subscription state | malformed/foreign no-write tests | MS-OXWSNTIF §3.1.4.4 | Returns success without a durable mutation because the replay cursor is canonical truth |
| Persona | `FindPeople` | partial | Existing address-book/contact projection API; linked-person aggregation only if later documented | accounts, contacts, grants; no Exchange persona table | SOAP account/contact projection, tenant/access isolation, unsupported linked-person fields | EWS persona docs | Returns stateless bounded personas for visible same-tenant accounts and accessible contacts |
| Persona | `GetPersona` | partial | Existing address-book/contact projection API; linked-person fetch only if later documented | accounts, contacts, grants; no Exchange persona table | stateless id fetch, stale/invisible id not found, tenant/access isolation | EWS persona docs | Resolves only LPE stateless `persona:account:{uuid}` and `persona:contact:{uuid}` ids |
| Retention | `GetUserRetentionPolicyTags` | partial | Existing retention policy API aligned with managed-folder creation eligibility | retention_policy_tags, account_retention_policy_assignments | tag listing, hidden assigned tag, no-policy tests | retention docs | Returns active same-tenant visible tags plus the authenticated account's assigned default tag; `CreateManagedFolder` uses the same same-tenant visible/assigned boundary for custom and personal folder tags |
| Service configuration | `GetServiceConfiguration` | partial | Existing service configuration API | tenant feature flags and policy state | mail tips/protection/UM config tests | service configuration docs | Returns only actually implemented service config |
| Sharing | `CreateItem` with `AcceptSharingInvitation` | partial | Existing sharing invitation accept API | sharing invitations, contact/calendar grants, audit | accept, decline, expired token, permission tests | sharing docs | Accepts supported calendar/contact shares through canonical grants |
| Sharing | `GetSharingFolder` | partial | Existing bounded sharing folder API | sharing grants and canonical folder projections | sharing folder visibility tests | sharing docs | Returns canonical shared folder metadata without Exchange remote-share state |
| Sharing | `GetSharingMetadata` | partial | Existing bounded sharing metadata API | sharing grants and canonical invitation metadata | sharing metadata tests | sharing docs | Returns canonical sharing metadata only for supported share shapes |
| Sharing | `RefreshSharingFolder` | partial | Existing sharing sync refresh API | sharing grants, canonical shared folder metadata | refresh, revoked share, stale data tests | sharing docs | Refreshes canonical shared folder metadata |
| Synchronization | `SyncFolderHierarchy` | partial | Existing hierarchy projection plus durable opaque cursor snapshots | mailboxes, contact_books, calendars, task_lists, public_folders, change log, ews_sync_cursors | incremental hierarchy, deletes, token/account/scope rejection, paging, restart-resume tests | sync docs | Emits bounded current hierarchy changes with opaque account- and scope-bound cursor state persisted for 30 days; rows contain only derived protocol snapshots |
| Synchronization | `SyncFolderItems` | partial | Existing item projection plus retained canonical mailbox-item replay cursors and durable opaque snapshots | canonical item tables, mail_change_log, tombstones, ews_sync_cursors | incremental creates/updates/deletes, cursor expiry, account/scope rejection, restart-resume tests | sync docs | Newly issued collaboration, public-folder, and mailbox states are 30-day opaque account/scope-bound cursors; the initial mailbox page projects bounded current items, while later pages and resumes replay retained account/folder-bound changes |
| Time zone | `GetServerTimeZones` | partial | Explicit `lpe-ews-time-zones-v1` compact catalog; full timezone catalog API if parity is required | timezone catalog table or generated data source if adopted | compact catalog selection and invalid request tests | timezone docs | With `ReturnFullTimeZoneData="false"`, returns both compact `UTC` and `Europe/Berlin` definitions when unfiltered, or exactly one case-sensitive known id when filtered; no full Exchange corpus or transition payload |
| Unified Messaging | `DisconnectPhoneCall` | partial | Existing bounded Unified Messaging call-state API | unified_messaging_calls | disconnect call tests | UM docs | Cancels active same-account canonical calls only; no PBX or voicemail transport control |
| Unified Messaging | `GetPhoneCallInformation` | partial | Existing bounded Unified Messaging call-state API | unified_messaging_calls | call info tests | UM docs | Returns same-account canonical call state only |
| Unified Messaging | `PlayOnPhone` | partial | Existing bounded Unified Messaging play-request API | unified_messaging_calls plus canonical voicemail message ids if present | play request and auth tests | UM docs | Creates canonical play-on-phone call requests without real telephony control |
| Unified Contact Store | `AddNewImContactToGroup` | partial | Existing contact create plus IM group membership API | contact_books role im_contact_list, contacts, contact_groups, contact_group_members | add new IM contact, group membership, tenant tests | UCS/contact docs | Creates a canonical contact in the IM list and links it to a canonical IM group |
| Unified Contact Store | `AddImContactToGroup` | partial | Existing IM group membership API over visible contacts/accounts | contacts, accounts, contact_groups, contact_group_members | add existing contact/account tests, stale id tests | UCS/contact docs | Links visible canonical contacts/accounts to canonical IM groups |
| Unified Contact Store | `AddImGroup` | partial | Existing IM group API | contact_books role im_contact_list, contact_groups | add group tests | UCS/contact docs | Creates canonical IM groups without Exchange UCS-only state |
| Unified Contact Store | `AddNewTelUriContactToGroup` | partial | Existing IM group membership API for tel URI external members | contact_groups, contact_group_members | add tel URI tests | UCS/contact docs | Adds tel URI members to canonical IM groups |
| Unified Contact Store | `AddDistributionGroupToImList` | partial | Existing address-book distribution-list projection plus IM group membership API | aliases/directory projection, contact_groups, contact_group_members | add distribution group, tenant isolation tests | UCS/contact docs | Adds only visible same-tenant distribution-list addresses to canonical IM groups |
| Unified Contact Store | `GetImItemList` | partial | Existing IM list projection API | contact_books role im_contact_list, contact_groups, contact_group_members | list retrieval tests | UCS/contact docs | Lists canonical IM groups and members |
| Unified Contact Store | `GetImItems` | partial | Existing IM member projection API | contacts, accounts, contact_groups, contact_group_members | item retrieval tests | UCS/contact docs | Reads canonical IM contacts, accounts, tel URIs, and distribution-list members |
| Unified Contact Store | `RemoveContactFromImList` | partial | Existing IM membership delete API | contact_group_members | remove contact tests | UCS/contact docs | Removes contact/account membership from canonical IM groups |
| Unified Contact Store | `RemoveImContactFromGroup` | partial | Existing IM membership delete API | contact_group_members | remove from group tests | UCS/contact docs | Removes contact/account membership from one canonical IM group |
| Unified Contact Store | `RemoveDistributionGroupFromImList` | partial | Existing IM membership delete API | contact_group_members | remove distribution group tests | UCS/contact docs | Removes canonical distribution-list membership rows |
| Unified Contact Store | `RemoveImGroup` | partial | Existing IM group delete API | contact_groups, contact_group_members | remove group tests | UCS/contact docs | Deletes canonical IM groups and memberships |
| Unified Contact Store | `SetImGroup` | partial | Existing IM group update API | contact_groups | rename/update group tests | UCS/contact docs | Updates canonical IM group display names |
| User configuration | `CreateUserConfiguration` | partial | Existing user configuration blob API | account_client_configurations keyed by account/folder/name | create account and mailbox scoped blobs, dictionary/XML/binary payload tests | user configuration docs | Stores bounded user config in canonical `account_client_configurations` |
| User configuration | `DeleteUserConfiguration` | partial | Existing user configuration blob API | account_client_configurations | delete and missing-row tests | user configuration docs | Deletes canonical user config blobs |
| User configuration | `GetUserConfiguration` | partial | Existing user configuration blob API | account_client_configurations keyed by account/folder/name | get all/selective properties, missing-row tests | user configuration docs | Returns bounded dictionary, XML, and binary payloads from canonical storage |
| User configuration | `UpdateUserConfiguration` | partial | Existing user configuration blob API | account_client_configurations, audit | update dictionary/XML/binary payload tests | user configuration docs | Updates canonical user config blobs and advances canonical modseq |

## Current Partial Dispatcher Surface

The current EWS dispatcher in `crates/lpe-exchange/src/service.rs` routes all 96 Microsoft catalog operation names to concrete bounded handlers. Those handlers cover mailbox item/folder/attachment paths, managed retention folders, reminders, conversations, utilities, availability, bulk transfer, delegates, inbox rules, mail apps, mail tips, notifications, retention policy tags, service configuration, sharing, synchronization, time zones, Unified Contact Store, Unified Messaging, and user configuration.

These are all `partial` against Microsoft parity because LPE intentionally maps only bounded Exchange-compatible behavior to canonical state. The automated gate records 96/96 Microsoft catalog operations with named SOAP test evidence (100.0%), 96/96 canonical behavioral implementations (100.0%), and 0/96 explicit unsupported SOAP-covered operations (0.0%).

## ConvertId Opaque-Id Strategy

`ConvertId` accepts canonical `EwsId` values for supported object families and deterministic opaque alternate ids generated by LPE. The opaque payload is stateless: `LPEEWS1.` followed by URL-safe base64 without padding of `lpeews:v1:<canonical-family-id>`. `HexEntryId` is the uppercase hex encoding of that same opaque token. No Exchange-local identity table is introduced; the canonical prefixed id remains authoritative.

Supported canonical families are `message:{uuid}`, `mailbox:{uuid}`, `contact:{uuid}`, `event:{uuid}`, `task:{uuid}`, `attachment:{message_uuid}:{attachment_uuid}`, `public-folder:{uuid}`, and `public-folder-item:{uuid}`. Public-folder folders and items are returned as `AlternatePublicFolderId` and `AlternatePublicFolderItemId`; the other families are returned as `AlternateId`.

## Explicit Unsupported Dispatcher Surface

The dispatcher currently has no Microsoft EWS catalog operation branch whose only behavior is an explicit unsupported response.

The automated parity gate still keeps the unsupported-reason table explicit. If a future operation is intentionally downgraded to unsupported, it must carry a tracked reason and return a parseable EWS unsupported response.

## Highest-Value EWS Parity Work

Priority should favor practical Outlook and native-client behavior before broad Exchange feature emulation:

1. Widen `SendItem` only where real clients require additional Exchange disposition options, while preserving canonical submission.
2. Widen `UpdateInboxRules` only for rule shapes that can map to canonical Sieve behavior.
3. Widen reminder action support only for additional actions that map to canonical reminder fields.
4. Add explicit canonical room-list membership only if clients require more than the computed tenant room/resource-list projection.
5. Harden notification long-poll/streaming behavior while keeping replay based on canonical `mail_change_log`.
6. Durable user-configuration blobs only if real Outlook/EWS clients require them.

## Verification

The current EWS tests include coverage proving folder operations, managed retention folder creation, `SendItem`, delegation, user configuration, inbox rules, reminders, rooms, sharing, eDiscovery, bulk transfer, mail apps, Unified Messaging, `MarkAllItemsAsRead`, conversations, `GetMailTips`, `GetServiceConfiguration`, retention tags, utilities, `ConvertId`, and notification replay route through canonical store APIs or stateless compatibility codecs rather than Exchange-only folder, `Sent`, `Outbox`, delegate, user-config, rule, reminder, room-list, mail-tip, read-state, id, or notification state.

`crates/lpe-exchange/src/tests/ews.rs::ews_catalog_gate_covers_documented_operations_and_unsupported_gaps` gates the full documented EWS operation catalog from a local Microsoft catalog snapshot and verifies that `docs/architecture/ews-operation-contract.md` still matches it. Every documented operation name must name an existing SOAP test. Canonical implementations count toward behavioral coverage; explicit unsupported rows also require a tracked reason and a parseable unsupported SOAP assertion.
